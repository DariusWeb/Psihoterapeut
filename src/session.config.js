// Session facts quoted in copy. The Booking page and the Home FAQ both state them, so they
// live here rather than drifting apart in two components.
export const SESSION_MINUTES = 50
export const SESSION_PRICE_RON = 250
