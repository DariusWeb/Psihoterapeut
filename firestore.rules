rules_version = '2';

// Only the Worker writes here (newsletter sign-up log, guide orders), through a service account
// that bypasses these rules — so every client read and write stays sealed.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
