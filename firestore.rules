rules_version = '2';

// Consent proof now lives in Brevo, recorded by its double opt-in confirmation. Nothing
// writes here any more, so the collection is sealed rather than left open to anonymous creates.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
